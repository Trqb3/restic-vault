{
  "name": "restic-vault",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "start": "npm run start --workspace=backend",
    "check": "svelte-kit sync && svelte-check --tsconfig ./frontend/tsconfig.json"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
